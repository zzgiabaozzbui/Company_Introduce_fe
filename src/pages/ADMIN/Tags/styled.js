import styled from "styled-components"

export const TagsStyled = styled.div`
  .tags-search {
    position: sticky;
    top: 10px;
    padding-top: 0px; //30 sticky
    background: #fff;
    margin-bottom: 0px;
    z-index: 10;
  }
  .form-insert {
    padding: 20px;
    background: #f7f7f7;
    border-radius: 8px;
    position: sticky;
    top: 82px;
    height: calc(100vh - 100px);
  }
`
